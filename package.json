{
  "name": "auto-deploy-action",
  "version": "1.0.0",
  "description": "One-click deployment to GitHub Pages with Jekyll support",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  },
  "license": "MIT"
}
